#include<stdio.h>
int main()
{
    int i, mult, p;
    printf("Digite qt de perguntas: ");
    scanf("%d", &p);
    for (i=p; i <= p; i=i+p)
    {
        
        printf("Digite um numero: ");
        scanf("%d", &mult);
        printf("Mult de 3= %d ", mult*3);
    }
    
}
